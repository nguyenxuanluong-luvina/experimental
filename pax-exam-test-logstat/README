This project is for reproduce redmine #350 problem. 
1. First, you have to build logstat bundle (do "mvn install" to install built bundle into your local repository).
   There is 1 problem in logstat project, it is the version of osgi core dependency, the version of org.osgi.core dependency have to be changed into "4.2.0".
